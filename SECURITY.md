# Security policy

Connector 插件不应被视为可信代码。Merge 必须把插件当成不可信数据，并在宿主层执行以下限制：

- 安装、更新和每次请求都重新验证清单。
- 不允许插件直接进行网络请求；所有请求都通过宿主代理。
- 不接受脚本、表达式求值、动态 Swift、JavaScript、Wasm 或命令。
- 不向插件代码提供认证信息和用户隐私数据。v2 的认证由宿主 App 管理：令牌只存 Keychain，插件只能声明服务和回调规则。
- 不接受插件声明 `Cookie`、`Authorization`、`Host` 或代理认证请求头；认证请求头由宿主根据已登录账户临时生成。
- 登录必须通过用户主动触发的系统授权界面，回调 Scheme 固定为 `merge`，并且登录地址必须属于已展示的域名权限。
- 域名权限变化必须重新向用户确认。
- 安装版本必须保存本地快照，远端更新不能静默改变已安装版本。
- 解析失败、超时或连续返回异常数据时应暂停插件，不得无限重试。
- 删除插件或用户退出账户时必须删除对应 Keychain 项；不得把令牌写入 UserDefaults、日志、文章内容或同步数据。

GitHub 仓库和发布者身份不等于安全保证。发布者应启用双因素认证、保护发布标签，并为每个 Release 提供可复现的 `plugin.json`。
